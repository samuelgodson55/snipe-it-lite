{
  "name": "snipe-it-lite-tailwind-build",
  "version": "1.0.0",
  "private": true,
  "description": "Local Tailwind CSS build for Snipe-IT Lite's frontend/ -- compiles frontend/css/tailwind.css from the HTML/JS class names instead of pulling Tailwind from a CDN at runtime.",
  "scripts": {
    "build": "tailwindcss -i input.css -o ../frontend/css/tailwind.css --minify",
    "watch": "tailwindcss -i input.css -o ../frontend/css/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
